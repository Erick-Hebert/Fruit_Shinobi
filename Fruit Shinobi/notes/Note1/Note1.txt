
slide_control = function() {	
	if (instance_place(x + 1, y, obj_wall) xor instance_place(x - 1, y, obj_wall) and !place_meeting(x, y + 1, obj_wall)) state = state_slide; 		
}

	if (xscale != 1) {
		var _val = -1;
	} else {
		var _val = 1;
	}
	
	if(slide_leave) {
		vspd = -max_vspd
		hspd = (max_hspd * -_val);
		xscale = -_val;
		jump_qt = 1;	
		slide_leave = 0;	
	}
	
	
slide_control();	
	
	